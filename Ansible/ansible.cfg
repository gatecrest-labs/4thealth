[defaults]
# Suppress deprecation warnings — INJECT_FACTS_AS_VARS will be removed in
# ansible-core 2.24; all templates use ansible_facts['key'] explicitly.
deprecation_warnings = False

# Use the local inventory file by default when running from this directory.
# Override with -i on the command line if needed.
inventory = inventory.local.yml
