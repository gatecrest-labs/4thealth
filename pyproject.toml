[project]
name = "4thealth"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0,<4",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "bcrypt>=4.1",
    "apscheduler>=3.11.3",
    "openpyxl>=3.1.5",
    "pysnmp>=7.1.27",
    "cryptography>=50.0.0",
    "pyzipper>=0.4.0",
    "paramiko>=5.0.0",
    "psutil>=7.2.2",
    "scp>=0.16.1",
]

[project.optional-dependencies]
prod = ["gunicorn>=22"]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.16",
]
